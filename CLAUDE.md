<!-- HELM BEGIN -->
Helm's operating rules, agent identities, and playbooks are authored once in `AGENTS.md` at the repo root — that file is the canonical, authoritative source of Helm's rules. If your host does not expand the `@path` import below, read `AGENTS.md` directly.

@AGENTS.md
<!-- HELM END -->
