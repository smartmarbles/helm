"""Helm orchestration runtime controller.

Host-neutral policy boundary for Copilot agent hooks. This package is greenfield
scaffolding established in spec015 Phase 1; downstream subpackages (contracts,
hooks, store, policy, fsm, gates, invariants, lifecycle, residual, audit) are
filled in by later tasks.
"""

__version__ = "0.1.0"
